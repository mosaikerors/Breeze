eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:6000/eureka/
server:
  port: 6001
spring:
  application:
    name: eureka-sign

  datasource:
    url: jdbc:mysql://localhost:3306/notus?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true
    username: root
    password: kty141
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: MYSQL
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect